{
  "name": "frontend",
  "version": "0.1.0",
  "dependencies": {
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:ci": "npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "test:ci": "CI=true npm test -- --passWithNoTests",
    "deploy:ci": "echo \"Uhuulz, I've just deployed to an S3 bucket\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
